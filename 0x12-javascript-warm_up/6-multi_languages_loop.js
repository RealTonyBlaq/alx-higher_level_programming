#!/usr/bin/node

/* Script prints strings using a loop */

const myArray = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < myArray.length; i++) {
  console.log(myArray[i]);
}
